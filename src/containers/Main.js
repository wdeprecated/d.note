import React from 'react';
import style from './Main.scss'

import { Navigator } from 'components'

const Main = () => {
    return (
        <main>
            <Navigator/>
        </main>
    );
};

export default Main;